#ifndef CROSS_CORRELATION_H
#define CROSS_CORRELATION_H
#include <stdlib.h>

int cross_correlation(const double *signal1, size_t size1, const double *signal2, size_t size2);

#endif	  // CROSS_CORRELATION_H
